version: "3.8"

services:
  redis:
    container_name: redis
    image: "redis"

  app:
    container_name: exmoney-app
    image: cuonvc/exmoney:staging
    build: .
    environment:
      - TZ=Asia/Ho_Chi_Minh
    ports:
      - "8081:8081"